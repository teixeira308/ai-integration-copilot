version: "3.9"
services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      PORT: 8080
      OLLAMA_MODEL: qwen2.5-coder
      OLLAMA_BASE_URL: http://host.docker.internal:11434
      OLLAMA_TIMEOUT: 5m
